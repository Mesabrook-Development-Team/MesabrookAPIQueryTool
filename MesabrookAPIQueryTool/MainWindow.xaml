<Window x:Class="MesabrookAPIQueryTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:MesabrookAPIQueryTool"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="Mesabrook API Query Tool" Height="450" Width="800" d:DataContext="{d:DesignInstance Type={x:Type local:MainWindowViewModel}}">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid IsEnabled="{Binding IsInitialized}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <WrapPanel Grid.Column="0" VerticalAlignment="Center" Height="auto" Margin="0,4" IsEnabled="{Binding IsSignedIn}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="API:" Margin="4,0" />
                    <ComboBox x:Name="QueryAPI" Width="auto" ItemsSource="{Binding AllAPIs}" SelectedItem="{Binding SelectedAPI}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="CSV Headers:" Margin="4,0" />
                    <TextBox x:Name="QueryPath" Width="200" Text="{Binding CSVHeaders}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Path:" Margin="4,0" />
                    <TextBox Width="200" Margin="4,0" Text="{Binding QueryPath}" />
                    <Button Content="Query" Command="{Binding ExecuteQueryCommand}" />
                </StackPanel>
            </WrapPanel>

            <Button x:Name="SignIn" Content="Sign In" Grid.Column="1" Command="{Binding SignInCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="SignOut" Content="Sign Out" Grid.Column="1" Command="{Binding SignOutCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSignedIn}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <TreeView Grid.Row="1" ItemsSource="{Binding QueryResults}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type local:QueryResponseObject}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Key}" />
                        <TextBlock Text=": ">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasChildren}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding FormattedValue}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasChildren}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
            <TreeView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Expand All" IsChecked="{Binding AreNodesExpanded}" Command="{Binding ToggleNodeExpandedCommand}" />
                </ContextMenu>
            </TreeView.ContextMenu>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding DataContext.AreNodesExpanded, RelativeSource={RelativeSource AncestorType=Window}}" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</Window>
